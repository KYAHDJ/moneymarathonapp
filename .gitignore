node_modules/

# Android build outputs
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/release/
android/captures/
android/.cxx/
*.apk
*.aab
!apk/app-debug.apk

# release signing — the keystore and its password are irreplaceable; losing
# them means never being able to publish an update to this app again, and
# leaking them means anyone else could. Never committed, ever.
android/keystore.properties
android/keystore/

# IDE / tooling
.idea/
.claude/
.vscode/
*.iml

.DS_Store
Thumbs.db
